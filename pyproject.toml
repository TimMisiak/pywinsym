[tool.poetry]
name = "pywinsim"
version = "0.1.0"
description = ""
authors = ["Tim Misiak <tim.misiak@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pdbparse = "^1.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
